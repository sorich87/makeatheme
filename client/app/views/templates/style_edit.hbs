<form>
  <p class="x-choice">
    <label>Current Element:</label>
    <b>{{selector}}</b>
  </p>
  <ul class="x-rules">
    {{#each rules}}
    <li><input name="property" value="{{property}}" />: <input name="value" value="{{value}}" /></li>
    {{/each}}
  </ul>
  <button>Add custom style</button>
</form>
